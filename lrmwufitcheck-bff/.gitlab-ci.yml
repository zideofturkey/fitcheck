stages:
  - deploy
  - build
  - test

build-and-deploy-server:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - sudo bash -c "docker build -f Dockerfile -t bff-service:latest ."
    - sudo bash -c 'cd undefined/fitcheck/  && docker compose up -d bff-service'
  only:
    - main
# Notice: This file is generated by Mindbricks and formatted by prettier-plugin-yaml
