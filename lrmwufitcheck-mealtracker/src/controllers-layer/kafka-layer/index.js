const mealTrackerHandler = require("./mealTracker");
const topics = require("./topics");
const { KafkaListener } = require("common");

const startKafkaListeners = async () => {};

module.exports = startKafkaListeners;
